spring.application.name=challenge-literatura-jokerp515
spring.datasource.url=jdbc:postgresql://${PostgreDB_HOST}/${PostgreDB_NAME}
spring.datasource.username=${PostgreDB_USER}
spring.datasource.password=${PostgreDB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
hibernate.dialect=org.hibernate.dialect.HSQLDialect
spring.jpa.hibernate.ddl-auto=update
